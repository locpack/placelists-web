export enum Namespace {
  Auth = "Auth",
  Users = "Users",
  Packs = "Packs",
  Places = "Places",
}

export enum PackStatus {
  None = "NONE",
  Followed = "FOLLOWED",
  Created = "CREATED",
}

export enum AuthStatus {
  No = "NO",
  Yes = "YES",
}
