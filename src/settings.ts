export enum ButtonType {
  Primary,
  Secondary,
  Destructive,
}

export enum InputType {
  Number = "number",
  Search = "search",
  Text = "text",
  Email = "email",
  Password = "password",
}

export enum Route {
  Home = "/home",
  Login = "/signin",
  Register = "/signup",
  Discover = "/discover",
  Saved = "/saved",
  Profile = "/profile",
}
